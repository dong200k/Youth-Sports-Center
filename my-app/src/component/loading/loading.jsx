import React from 'react';
import ReactLoading from 'react-loading';
 
const Example = ({ type, color }) => (
    <ReactLoading type={'bars'} color={'#bc8f8f'} height={'40%'} width={'40%'} />
);
 
export default Example;